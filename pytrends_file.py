
# date
yesterday=(datetime.now()-timedelta(days=1)).date()
week_ago=(datetime.now()-timedelta(days=8)).date()
dates=str(week_ago) + " " + str(yesterday)# keywords
keywords=[ ["keyword"],cat_number,["keyword"],cat_number...etc]
df = tracking_in_time_keywords(companies)
